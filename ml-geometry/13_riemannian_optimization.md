# 13 — Riemannian Optimization

**Cross-references.** `ml-geometry/NOTATION_ML.md` (typing §1.4, citation key
§3.1–3.2); `notes/04_riemannian_metrics_and_curvature.md` §1.1 (musical
isomorphism $\sharp$, reused directly for $\operatorname{grad}f$), §3
(geodesics, $\exp$, reused at recognition depth); `notes/02_manifolds_and_tangent_spaces.md`
(tangent spaces); `01_prerequisite_systematic_review_for_GMT_Topology.md`
§4.7, §13.5 (PCA/spectral geometry, completed here as a worked Riemannian-optimization
example).

---

## 0. Standing Definitions

$$
M \quad \text{a Riemannian manifold (`notes/NOTATION.md' §2.2–2.3)}, \qquad f:M\to\mathbb{R} \quad C^1, \qquad x\in M.
$$

$$
\operatorname{grad}f(x) := \sharp_x(df_x) \in T_xM \quad \text{(the Riemannian gradient — `notes/04' §1.1's $\sharp$, no new definition)}.
$$

$$
R_x : T_xM\to M \quad \text{a retraction}: \quad R_x(0)=x, \qquad dR_x|_0 = \operatorname{id}_{T_xM}.
$$

$$
\operatorname{St}(n,p) := \{X\in\mathbb{R}^{n\times p}:X^\top X=I_p\}, \qquad \operatorname{Gr}(n,p) := \operatorname{St}(n,p)/O(p), \qquad S^{n-1} = \operatorname{St}(n,1).
$$

---

## 1. Why Optimize on a Manifold

Many optimization problems in ML carry a **geometric constraint** that is
naturally a smooth manifold rather than an affine subspace: unit-norm
vectors ($S^{n-1}$), orthonormal frames ($\operatorname{St}(n,p)$),
subspaces ($\operatorname{Gr}(n,p)$), symmetric positive-definite matrices
(covariance estimation), or low-rank matrices. Riemannian optimization
treats the constrained problem $\min_{x\in M}f(x)$ intrinsically, rather
than via penalty terms or post-hoc projection after an unconstrained step.

> **Type-check — the Riemannian gradient needs a metric; the ordinary
> projected gradient is a specific choice of one.** $df_x\in T_x^*M$ exists
> for any $C^1$ $f$, metric or not (`notes/02` §5.2). $\operatorname{grad}f(x)
> =\sharp_x(df_x)$ requires a Riemannian metric $g$ on $M$ to make sense of
> $\sharp_x$ (`notes/04` §1.1, Proposition 1.2). For $M\subseteq\mathbb{R}^N$
> an embedded submanifold with the metric induced from the ambient Euclidean
> inner product, $\operatorname{grad}f(x)$ turns out to equal
> $\operatorname{Proj}_{T_xM}\big(\nabla_{\mathbb{R}^N}f(x)\big)$ (orthogonal
> projection of the ambient Euclidean gradient onto the tangent space) — but
> this is a *consequence* of the induced metric being the Euclidean one
> restricted to $T_xM$, not the definition of the Riemannian gradient
> itself, which is metric-first (Definition in §0).

---

## 2. Retractions and the Riemannian Gradient-Descent Iteration

The Riemannian gradient descent iteration is

$$
x_{k+1} = R_{x_k}\big(-t_k\,\operatorname{grad}f(x_k)\big), \qquad t_k>0 \text{ a step size}.
$$

$R_x$ substitutes for the exponential map $\exp_x$ (`notes/04` §3, at
recognition depth here) — $\exp_x$ is the "exact" choice ($R_x=\exp_x$
always satisfies the retraction axioms of §0, since geodesics have unit
initial-velocity-derivative by construction) but is often expensive to
compute exactly; a retraction is any computationally cheaper local
surrogate agreeing with $\exp_x$ to first order at $0$.

| Manifold | A standard retraction |
|---|---|
| $S^{n-1}$ | $R_x(v) = (x+v)/\lVert x+v\rVert$ (normalize) |
| $\operatorname{St}(n,p)$ | $R_X(V) = \operatorname{qf}(X+V)$ (Q-factor of a QR decomposition) |
| $\operatorname{Gr}(n,p)$ | induced from a $\operatorname{St}(n,p)$ retraction via the quotient |
| SPD matrices | $R_X(V) = X+V+\tfrac12VX^{-1}V$ or the matrix exponential retraction |

---

## 3. Anchor Proof: Riemannian Gradient Descent with Backtracking Converges to a Stationary Point

**Theorem 3.1.** Let $M$ be a Riemannian manifold with retraction $R$, let
$f\in C^1(M)$ be bounded below, and fix a starting point $x_0\in M$. Assume
the **retraction-smoothness condition**: there is $L>0$ such that

$$
f\big(R_x(-t\operatorname{grad}f(x))\big) \ \le\ f(x) - t\,\lVert\operatorname{grad}f(x)\rVert_g^2 + \tfrac{L}{2}\,t^2\,\lVert\operatorname{grad}f(x)\rVert_g^2
$$

for every $x$ in the sublevel set $\{f\le f(x_0)\}$ and every $t\in(0,t_0]$
— the curved-space analogue of the Euclidean descent lemma for functions
with $L$-Lipschitz gradient, satisfied e.g. whenever $M$ is compact and
$f$, $R$ are $C^2$ (Boumal, ISRO, Ch. 4, where it appears as a
Lipschitz-type-gradient assumption along retraction curves). Suppose
$\{x_k\}$ is generated by $x_{k+1}=R_{x_k}(-t_k\operatorname{grad}f(x_k))$
where $t_k$ is chosen by Armijo backtracking on the pulled-back cost
$\hat f_k(t):=f(R_{x_k}(-t\operatorname{grad}f(x_k)))$: the largest
$t_k\in\{t_0,t_0\beta,t_0\beta^2,\dots\}$ (for fixed $t_0>0,\beta\in(0,1)$)
satisfying

$$
\hat f_k(t_k) \le f(x_k) - c\,t_k\,\lVert\operatorname{grad}f(x_k)\rVert_g^2, \qquad c\in(0,1) \text{ fixed}.
$$

Then every backtracking search terminates with

$$
t_k \ \ge\ t_{\min} := \min\big(t_0,\ 2\beta(1-c)/L\big) \ >\ 0 \qquad \text{for every } k,
$$

and $\lim_{k\to\infty} \lVert\operatorname{grad}f(x_k)\rVert_g = 0$.

*Proof.* Fix $k$ and consider $\hat f_k:(-\varepsilon,\varepsilon)\to\mathbb{R}$,
$\hat f_k(t) = f(R_{x_k}(-t\operatorname{grad}f(x_k)))$ — an ordinary
single-variable function, obtained by pulling $f$ back along the curve
$t\mapsto R_{x_k}(-t\operatorname{grad}f(x_k))$ in $M$. By the chain rule
and the retraction axiom $dR_{x_k}|_0=\operatorname{id}_{T_{x_k}M}$,

$$
\hat f_k'(0) = df_{x_k}\big(-\operatorname{grad}f(x_k)\big) = -g_{x_k}\big(\operatorname{grad}f(x_k),\operatorname{grad}f(x_k)\big) = -\lVert\operatorname{grad}f(x_k)\rVert_g^2,
$$

using the defining property of $\operatorname{grad}f=\sharp(df)$
(`notes/04` §1.1: $g(\operatorname{grad}f,X)=df(X)$ for every $X$, here
$X=-\operatorname{grad}f(x_k)$). Abbreviate $g_k := \operatorname{grad}f(x_k)$;
so $\hat f_k(0)=f(x_k)$ and the first-order behavior of $\hat f_k$ at $0$ is
exactly $-\lVert g_k\rVert_g^2$.

*Step 1: backtracking terminates, with $t_k \ge t_{\min}$.* First,
$f(x_k)\le f(x_0)$ for every $k$, by induction: each accepted step satisfies
the Armijo inequality, whose right-hand side is $\le f(x_k)$. So the
retraction-smoothness condition applies at every iterate: for $t\in(0,t_0]$,

$$
\hat f_k(t) \ \le\ f(x_k) - t\lVert g_k\rVert_g^2 + \tfrac{L}{2}t^2\lVert g_k\rVert_g^2 \ =\ f(x_k) - t\Big(1-\tfrac{L}{2}t\Big)\lVert g_k\rVert_g^2.
$$

The Armijo condition $\hat f_k(t)\le f(x_k) - c\,t\lVert g_k\rVert_g^2$
therefore holds whenever $1-\tfrac{L}{2}t \ge c$, i.e. whenever $t \le
2(1-c)/L$ (and trivially at any $t$ if $g_k=0$). So a candidate $t_0\beta^j$
can fail the test only while $t_0\beta^j > 2(1-c)/L$: either $t_0$ itself is
accepted, or the search stops at the first candidate $\le 2(1-c)/L$ — which,
being exactly one contraction step below a rejected candidate $> 2(1-c)/L$,
is $> 2\beta(1-c)/L$. Hence backtracking terminates after finitely many
contractions with

$$
t_k \ \ge\ t_{\min} = \min\big(t_0,\ 2\beta(1-c)/L\big) \ >\ 0, \qquad \text{uniformly in } k.
$$

*Step 2: telescoping.* The accepted step gives

$$
f(x_k) - f(x_{k+1}) = f(x_k)-\hat f_k(t_k) \ \ge\ c\,t_k\,\lVert g_k\rVert_g^2 \ \ge\ c\,t_{\min}\,\lVert g_k\rVert_g^2 \ \ge\ 0.
$$

Sum over $k=0,\dots,K-1$: the left side telescopes to $f(x_0)-f(x_K)$,
bounded above by $f(x_0)-\inf_M f<\infty$ since $f$ is bounded below. So

$$
c\,t_{\min}\sum_{k=0}^{K-1} \lVert g_k\rVert_g^2 \ \le\ f(x_0)-\inf_M f \qquad \text{for every } K,
$$

hence the nonnegative series $\sum_k \lVert g_k\rVert_g^2$ converges, and in
particular its terms tend to zero:
$\lVert\operatorname{grad}f(x_k)\rVert_g \to 0$. $\blacksquare$

> **Type-check — why the smoothness hypothesis is not decoration.** An
> earlier draft of this document claimed $\liminf_k\lVert\operatorname{grad}
> f(x_k)\rVert_g = 0$ from $f\in C^1$ + bounded below alone. That is not
> enough: for a merely-$C^1$ objective, the accepted step sizes $t_k$ can
> shrink to $0$ fast enough that $\sum_k t_k\lVert g_k\rVert_g^2 < \infty$
> carries no information about $\lVert g_k\rVert_g$ itself. The uniform
> lower bound $t_k \ge t_{\min}$ — which is exactly what the
> retraction-smoothness condition buys, via the explicit acceptance
> threshold $2(1-c)/L$ in Step 1 — is the load-bearing step, and it is the
> curved-space image of the Euclidean fact that gradient-descent theory
> runs on *Lipschitz gradients*, not on bare differentiability.

*(This pullback-to-the-tangent-space strategy is the standard proof
technique for Riemannian descent-type convergence; Absil–Mahony–Sepulchre,
Ch. 4, Thm 4.3.1 for the general statement; Boumal, ISRO, Ch. 4, whose
Lipschitz-type-gradient assumption and sufficient-decrease lemma the
hypothesis and Step 1 above follow.)*

> **What this buys you.** The manifold-specific content of the proof is
> confined to two places: the chain-rule computation of $\hat f_k'(0)$,
> which uses only the retraction axiom and the *definition* of
> $\operatorname{grad}f$ as $\sharp(df)$ (`notes/04` §1.1); and the
> retraction-smoothness hypothesis, the curved-space stand-in for the
> Euclidean descent lemma $f(x+v)\le f(x)+\langle\nabla f(x),v\rangle+
> \tfrac{L}{2}\lVert v\rVert^2$. Everything else is the flat Armijo
> argument, now carried out in full rather than imported. This is exactly
> the payoff of having built $\sharp$ properly in `notes/04`: the "hard
> part" of Riemannian optimization theory is not reinventing descent-lemma
> analysis on a curved space, but correctly identifying that pulling back
> along a retraction reduces it to the flat case.

---

## 4. Worked Example: Rayleigh Quotient Minimization on $S^{n-1}$ Recovers PCA

**Setup.** Let $\Sigma\in\mathbb{R}^{n\times n}$ be symmetric, and consider
$f:S^{n-1}\to\mathbb{R}$, $f(x)=x^\top\Sigma x$ (the Rayleigh quotient,
restricted to unit vectors). This is exactly the smallest-eigenvector
problem underlying PCA (`01` §13.5, `01` §4.7's proof-anchor menu), now
posed as Riemannian optimization on $S^{n-1}\subseteq\mathbb{R}^n$ with its
induced Euclidean metric.

**Proposition 4.1 (depth B).** $\operatorname{grad}f(x) = 2\big(\Sigma x -
(x^\top\Sigma x)\,x\big) = 2\,\operatorname{Proj}_{T_xS^{n-1}}(\Sigma x)$,
where $T_xS^{n-1}=\{v\in\mathbb{R}^n:x^\top v=0\}$.

*Proof.* The ambient Euclidean gradient of $\tilde f(x):=x^\top\Sigma x$
(extended to all of $\mathbb{R}^n$) is $\nabla\tilde f(x)=2\Sigma x$. Since
$S^{n-1}$ is embedded in $\mathbb{R}^n$ with the induced metric, the
Riemannian gradient of the restriction $f=\tilde f|_{S^{n-1}}$ is the
orthogonal projection of $\nabla\tilde f(x)$ onto $T_xS^{n-1}$ (the
embedded-submanifold specialization of $\operatorname{grad}f=\sharp(df)$
noted in §1's type-check): $\operatorname{Proj}_{T_xS^{n-1}}(v) = v -
(x^\top v)x$ for $x\in S^{n-1}$ (standard orthogonal projection onto the
hyperplane orthogonal to $x$), giving $\operatorname{grad}f(x) = 2\Sigma x -
2(x^\top\Sigma x)x$. $\blacksquare$

At a critical point, $\operatorname{grad}f(x)=0 \iff \Sigma x = (x^\top\Sigma
x)x$ — i.e. $x$ is a unit eigenvector of $\Sigma$ with eigenvalue
$x^\top\Sigma x$. Theorem 3.1 applied to $-f$ (to maximize, i.e. minimize
$-f$; its retraction-smoothness hypothesis holds automatically here, since
$S^{n-1}$ is compact and both $f$ and the normalization retraction are
smooth, giving the required second-order bound with a uniform $L$)
converges to a stationary point of the Rayleigh quotient, which is
exactly an eigenvector of $\Sigma$; the global maximizer recovers the
top eigenvector, matching `01` §13.5's PCA characterization — but now
obtained as a special case of the general Riemannian-descent convergence
guarantee of Theorem 3.1, rather than via the spectral theorem's closed-form
diagonalization directly.

---

## References Used

- **Absil–Mahony–Sepulchre** — Ch. 3 (retractions), Ch. 4 (Riemannian
  gradient descent, Thm 4.3.1 — the anchor proof of §3 follows this
  source's pullback-to-tangent-space strategy), Ch. 4.6 (the Rayleigh
  quotient / eigenvector example, cross-checked against the direct
  computation in §4).
- **Boumal, ISRO** — a modern, self-contained treatment of the same
  convergence argument (Ch. 4), and of retractions on $\operatorname{St}(n,p)$,
  $\operatorname{Gr}(n,p)$ (Ch. 7).
- **Bonnabel 2013** — the stochastic (Riemannian SGD) extension, cited at
  recognition depth, not needed for the deterministic convergence proof
  above.
- **`notes/04_riemannian_metrics_and_curvature.md`** §1.1 — the musical
  isomorphism $\sharp$ defining $\operatorname{grad}f$, used without
  modification throughout.

Full bibliographic data: `ml-geometry/NOTATION_ML.md` §3.
